<nav class="usable-navbar">
  <div class="usable-navbar-inner">
    <a href="/" class="usable-navbar-logo" aria-label="SereniStay Home">
      <img src=https://i.ibb.co/GTvBR2C/Sereni-Stays-light-1.png class="nav-logo-img" alt="SereniStay Logo" />
      <span class="brand-plain-text">SereniStay</span>
    </a>
    <input type="checkbox" id="usable-menu-toggle" class="usable-menu-toggle" aria-label="Toggle navigation menu" />
    <label for="usable-menu-toggle" class="usable-menu-icon" tabindex="0">
      <span></span><span></span><span></span>
    </label>
    <ul class="usable-navbar-menu">
      <li><a href="/">Home</a></li> 
      <li><a href="/pathlistings">Listings</a></li>

    </ul>
    
  </div>
</nav>

<style>
  .usable-navbar {
  background: #24243e; /* deep navy for high contrast */
  color: #fff;
  position: sticky;
  top: 0;
  z-index: 1200;
  box-shadow: 0 3px 12px -5px #16162837;
  font-family: 'Segoe UI', 'Montserrat', Arial, sans-serif;
}

.usable-navbar-inner {
  max-width: 1200px;
  margin: 0 auto;
  height: 65px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 16px;
}

.usable-navbar-logo {
  display: flex;
  align-items: center;
  text-decoration: none;
}

.nav-logo-img {
  height: 43px;
  width: 43px;
  border-radius: 50%;
  object-fit: cover;
  background: #fff;
  margin-right: 10px;
  border: 1.5px solid #eaeaea;
  transition: filter 0.2s;
}
.usable-navbar-logo:hover .nav-logo-img {
  filter: brightness(1.15) drop-shadow(0 0 6px #32d1b6a6);
}
.brand-plain-text {
  font-family: inherit;
  font-size: 1.55rem;
  font-weight: 700;
  color: #fff;
  letter-spacing: 1.2px;
}

.usable-navbar-menu {
  display: flex;
  align-items: center;
  list-style: none;
  margin: 0;
  padding: 0;
  z-index: 1001;
}
.usable-navbar-menu li {
  margin-left: 26px;
}

.usable-navbar-menu li a {
  color: #fff;
  text-decoration: none;
  font-weight: 600;
  font-size: 1.05rem;
  padding: 7px 15px;
  border-radius: 6px;
  transition: background 0.18s, color 0.21s;
}

.usable-navbar-menu li a:focus,
.usable-navbar-menu li a:hover {
  background: #5ee7df;
  color: #24243e;
  outline: none;
}

.usable-menu-toggle { display: none; }
.usable-menu-icon {
  display: none;
  flex-direction: column;
  width: 31px;
  height: 24px;
  justify-content: space-between;
  cursor: pointer;
  z-index: 1201;
}
.usable-menu-icon span {
  height: 3px;
  width: 100%;
  background: #fff;
  border-radius: 1.9px;
  transition: all 0.29s;
  display: block;
}

@media (max-width: 820px) {
  .usable-navbar-inner { height: 56px; }
  .usable-menu-icon { display: flex; }
  .usable-navbar-menu {
    position: fixed;
    top: 56px;
    right: 0;
    width: 220px;
    height: calc(100% - 56px);
    background: #24243e;
    flex-direction: column;
    align-items: flex-start;
    padding: 30px 20px 0 20px;
    transform: translateX(110%);
    transition: transform 0.21s cubic-bezier(.45,.38,.36,1.13);
    box-shadow: -3px 0 12px -6px #32d1b69c;
  }
  .usable-navbar-menu li {
    margin: 14px 0 0 6px;
  }
  .usable-navbar-menu li a {
    width: 100%;
    font-size: 1.13rem;
    padding: 10px 0;
    text-align: left;
  }
  .usable-menu-toggle:checked + .usable-menu-icon span:nth-child(1) {
    transform: rotate(45deg) translate(4.8px,7px);
  }
  .usable-menu-toggle:checked + .usable-menu-icon span:nth-child(2) {
    opacity: 0;
  }
  .usable-menu-toggle:checked + .usable-menu-icon span:nth-child(3) {
    transform: rotate(-45deg) translate(6px,-6.2px);
  }
  .usable-menu-toggle:checked + .usable-menu-icon + .usable-navbar-menu {
    transform: translateX(0);
  }
}

</style>